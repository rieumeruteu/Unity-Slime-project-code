//박스 콜라이더를 지정해서 지정된 곳에 있으면 브금을 실행시키는 방법
public class Testbgm : MonoBehaviour
{
    bgmManager BGM;//bgmManager를 실행시켜 줄 변수명 BGM
    public int _playMusicTrack;

    void Start()
    {
        BGM=FindObjectOfType<bgmManager>();//bgmManager를 BGM에 넣어준다
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        BGM.Play(_playMusicTrack);//브금 실행
        this.gameObject.SetActive(false);//브금을 한번만 재생하도록 꺼준다
    }

   
}